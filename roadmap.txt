now
  *show message count against friends viewingpost

    BUGS
    posting a picture breaks the layout

  make posts / profile DRY
  loading states
  
  private messages
  unpin posts from own profile
  show user status on their profile
  add friend/ indicate from profile
  sounds when not focused
  highlight unfocused messages like facebook chat

  notifications (user has pinned your post)
  friend accept, block, unfriend people that friend you
  track route history!

  filepicker bug on new window open (ipad iphone)
  *is typing - don't think it needs it
  *show user count against message
  *fix subscriptions, stop sending the entire collection to the user
  *add friends and see what post they are viewing and their online/offline status
  add notifications and tagging in posts
  *create methods for inserts
  *use sessions for showing the image is ready to be posted
  figure out how to delete images that are never posted, as they are stored before posting
  *rebuild front end code
  homepage will probably have most active posts, most visited and most linked
  
  SECURE IT

next
  *add user profiles
  do something with the homepage
  *add a button on each message so a user can like fav it, so it gets like repinned/posted whatever on their own profile, like tumblr
  *friends list
  *~profile page listing activity
  *~view profile, save(pin), private message, add to friends

future
  setup amazon for image hosting
  paginate (infinite scroll?)
